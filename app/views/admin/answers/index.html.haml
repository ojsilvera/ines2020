%h1 Listing answers

%table
  %thead
    %tr
      %th Description
      %th
      %th
      %th

  %tbody
    - @answers.each do |replay|
      %tr
        %td= replay.description
        %td= link_to 'Show', admin_replay_path(replay)
        %td= link_to 'Edit', edit_admin_replay_path(replay)
        %td= link_to 'Destroy', admin_replay_path(replay), :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Replay', new_admin_replay_path
