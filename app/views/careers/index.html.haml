%h1 Listing careers

%table
  %thead
    %tr
      %th Name
      %th
      %th
      %th

  %tbody
    - @careers.each do |career|
      %tr
        %td= career.name
        %td= link_to 'Show', career
        %td= link_to 'Edit', edit_career_path(career)
        %td= link_to 'Destroy', career, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Career', new_career_path
