%h1 Listing category_answers

%table
  %thead
    %tr
      %th Replay
      %th Category
      %th
      %th
      %th

  %tbody
    - @category_answers.each do |category_replay|
      %tr
        %td= category_replay.replay.description
        %td= category_replay.category.name
        %td= link_to 'Show', category_replay
        %td= link_to 'Edit', edit_category_replay_path(category_replay)
        %td= link_to 'Destroy', category_replay, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Category replay', new_category_replay_path
