<%= form_with(model: poll_body, local: true) do |form| %>
  <% if poll_body.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(poll_body.errors.count, "error") %> prohibited this poll_body from being saved:</h2>

      <ul>
        <% poll_body.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
